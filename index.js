/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import MappedInApp from './MappedInApp';

AppRegistry.registerComponent(appName, () => MappedInApp);
